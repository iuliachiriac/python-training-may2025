"""This is a docstring"""

print("hello world")  # this is a comment
x = 5
print(x + 7)

# This is another comment
if x > 0:
    print("positive number")

long_string = "If we have a long logical line, we can split it into physical" \
              " lines by using the backslash at the end of the line so that" \
              " Python understands that the next physical line should be" \
              " considered as part of the current logical line."

print(long_string)

print("If we have a long logical line, we can split it into physical lines by"
      " using the backslash at the end of the line so that")

multiline_string = """text
Out[91]: 'hello'
type(text)
Out[92]: str"""
print(multiline_string)
